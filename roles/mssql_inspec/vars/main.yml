---
# MSSQL InSpec Role - Variables
# ==============================

# NOTE: mssql_tools_paths is now computed at runtime in main.yml
# based on execution mode (EE vs delegate). Path defaults are in defaults/main.yml.
# See: mssql_tools_paths_ee, mssql_tools_paths_delegate
